# PMKit Configuration

# OpenProject Configuration
OPENPROJECT_HOST_NAME=localhost:8080
OPENPROJECT_HTTPS=false
OPENPROJECT_SECRET_KEY_BASE=change_me_in_production_to_a_long_random_string

# Ports
PORT=8080

# Data Storage
# Defaults to a 'data' directory in the current folder if not specified
# DATA_DIR=./data
